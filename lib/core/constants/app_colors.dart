import 'package:flutter/material.dart';

class AppColors {
  // Primary Theme Colors
  static const Color darkNavy = Color(0xFF0D2B45);
  static const Color lightNavy = Color(0xFF163E5F);

  // Feature Colors (Timer/Tasks)
  static const Color primaryPurple = Color(0xFF9181CB);
  static const Color accentTeal = Color(0xFF2FA2B1);

  // Background & Surface Colors
  static const Color background = Color(0xFFF5F7F9);
  static const Color white = Colors.white;
  static const Color grey = Colors.grey;

  // Task Card Colors (As seen in your Dashboard)
  static const Color taskBlue = Color(0xFF8EDCE6);
  static const Color taskMediumBlue = Color(0xFF6799C1);
  static const Color taskPurple = Color(0xFF8E8CD8);
}
